# LMS Coach MVP Agent Guide

Follow `Rules.md` for project scope and technical boundaries.

Current implementation baseline:

- Monorepo with npm workspaces.
- API app in `apps/api`.
- Web app in `apps/web`.
- Shared TypeScript package in `packages/shared`.
- SQLite database managed through Prisma.
- Local uploads live under `apps/api/uploads`.
